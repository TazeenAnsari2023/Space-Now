{
  "version": 2,
  "installCommand": "npm install && npm install --prefix server",
  "functions": {
    "pages/api/[...path].ts": {
      "maxDuration": 30,
      "includeFiles": "server/**"
    }
  }
}
